# 20260910_预训练边界类型对比计划（23:02:48 UTC+08:00）

创建时间：20260910 22:52:20 UTC+08:00
最后修订时间：20260910 23:02:48 UTC+08:00

## 实验设计

采用现有 FineWeb 数据构建三个等规模训练集：semantic、random，以及两者各占 50% 的 mixed。三组共享 semantic 和 random 两套 dev/test，分别报告结果。原始来源划分保持固定，训练与评估来源隔离。三组模型种子均为 42、训练采样 seed 为 20260907、实验数据选择 seed 为 20260910；首轮为单种子比较。

AE 输入 X、LM 输入 X 与目标 Y 均限制为 32–2048 tokens；通过筛选保留原文，样本不再截断。按 X 长度使用 32–64、65–128、129–256、257–512、513–1024、1025–2048 六档，每档 AE/LM 等量。实际配额由全部来源的可用数量共同确定；跨来源的相同任务与相同 X/Y 去重后，按配置中的来源顺序保留首次出现者，并单独统计去重数量。

## 训练设置

基座为 Llama-2-7B-Chat，写入与读取窗口均为 4096。压缩率 2、4、8 等概率，memory 上限 4096。所有入选样本预先验证实际写入、读取与完整上下文评估长度。

长度课程在前 30% 优化器步数内线性过渡：六档初始概率 25%、25%、20%、15%、10%、5%，最终各 1/6。dev/test 使用固定面板。三组保持相同训练步数、优化器参数及模型种子。

每个长度档内按样本随机轮转，使等量任务和混合来源在完整轮转中保持配额比例。

训练使用 BF16、梯度检查点与梯度裁剪，按长度组织 microbatch。学习率采用 warmup 后余弦衰减，每组计划 20,000 步，microbatch 为 1，梯度累积 8 次，每步抽取 8 条，总计 160,000 次样本使用。学习率峰值 3e-5，前 600 步 warmup，此后余弦衰减至 3e-6；长度课程过渡为 6,000 步。每 1,000 步保存 checkpoint 并评估，每套 dev 面板 120 条，每 2,000 步对其中 12 条执行生成评估，初始化也记录固定基线。样本使用次数与数据集遍历轮数分别记录。

## 数据与运行组织

通用准备入口由配置提供来源目录、来源比例和长度约束，输出各 run 的实际样本及选择记录；沿用 JSONL 数据格式，训练器使用数据路径，来源选择由数据准备完成。共享评估集以名称到目录的映射传给训练和独立评估入口，两个来源分别记录 `dev/<名称>/*` 和 `test/<名称>/*`。三组使用物理 GPU 0、1，每个 run 独占一张卡；semantic 与 random 先并行执行，mixed 等待空闲卡，使用相同硬件和训练设置。累计监督 token 数与实际吞吐同时记录。

SwanLab project 为 `latent-working-memory`，三组使用共同 group，训练职责为 `train`。自动标签包括 `scope:main`、`method:latent-working-memory`、`data:fineweb`，实验标签为 `study:boundary-comparison`。数据配比及课程参数保存在 config。

## 准备状态

准备阶段已完成，用户已授权正式执行。三组各 157,320 条训练样本，每个任务、长度档 13,110 条；mixed 每档每任务的两个来源各 6,555 条。每套共享 dev 为 3,468 条（每档每任务 289 条），test 为 3,108 条（每档每任务 259 条）。每组训练使用 160,000 次样本，约相当于训练集总量的 1.02 倍；长度课程下各档实际遍历次数不同。

## 实现与启动文件

- 计划配置：`configs/v1/pretrain_boundary_comparison_a800.json`。
- 数据选择配置：`configs/data_preparation/boundary_comparison.json`。
- 数据选择入口：`python -m latent_working_memory.data_preparation.experiment`。
- 训练入口通过 `--evaluation-dirs` 接收命名验证集；独立评估使用同一映射，`--split test` 评估固定测试集。
- 启动文件：`artifacts/v1/experiment-plans/boundary-comparison-2048-20260910/`，包含三组训练命令、三组测试命令、`evaluation-dirs.json` 和环境检查结果。命令已生成，正式运行已经授权。

数据选择结果：`data/v1/boundary-comparison-2048-20260910/selection.json`。筛选检查确认来源隔离及所有压缩率、完整上下文基线的窗口预算；跨来源去除 train 19 条、test 1 条重复内容。80 项 v1 测试通过；CPU 集成测试覆盖双验证集及课程日程下的精确断点恢复。
